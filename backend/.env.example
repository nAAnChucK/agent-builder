ANTHROPIC_API_KEY=your_key_here

# Optional: enables web_search tool
BRAVE_API_KEY=your_key_here

# Auth — set both to enable HTTP Basic Auth (leave AUTH_PASSWORD empty to disable)
AUTH_USERNAME=admin
AUTH_PASSWORD=

# Production: path to Railway volume mount for persistent storage
# DATA_DIR=/data
